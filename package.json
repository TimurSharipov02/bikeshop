{
  "name": "veloterra-workshop",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Веломастерская Veloterra — рабочий процесс, веб-приложение",
  "scripts": {
    "build": "node scripts/build-html.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3"
  }
}
